package com.vro.compose.template

/**
 * Base interface for mapping logic used within a [VROTemplate].
 *
 * This interface can be implemented to define how data, states, or models
 * are transformed before being passed to the UI layer.
 *
 * It serves as an extension point for separating transformation logic
 * from rendering and state management in a clean architecture approach.
 */
interface VROTemplateMapper